/**
 * @description Logs the value "0" to the console when called. It is a constant
 * function, denoted by the keyword `const`, which implies it cannot be reassigned.
 * The arrow function syntax defines a concise way to declare a function that returns
 * no value.
 */
const fn = () => {
  console.log(0);
  
}
