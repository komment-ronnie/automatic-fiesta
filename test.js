/**
 * @description Logs the value "0" to the console when invoked. The function is
 * declared with a constant declaration (`const`) and is immediately invoked as an
 * arrow function, which means it can directly return values without explicitly
 * returning them using the `return` statement.
 */
const fn = () => {
  console.log(0);
  
}
