/**
 * @description Returns no value and simply logs the number 0 to the console when
 * invoked, but does not affect any external state or return any value.
 */
const fn = () => {
  console.log(0);
  
}
